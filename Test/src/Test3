#include<iostream>
#include<cstdlib>
using namespace std;

int main(){
	for (int i = 3; i > -4; i--) {
		for (int j = 0; j < abs(i); j++) {
			cout << " ";
		}
		for (int j = 0; j < 7 - 2 * abs(i); j++) {
			cout << abs(i) + 1;
		}
		cout << endl;
	}
}
