#include<iostream>
#include<math.h>
#include<stdlib.h>
using namespace std;

int main() {
	int N = 0;
	int i, temp;
	double r, l;
	cin >> r;
	l = floor(r);
	for (i = 0; i <= l; i++) {
		temp = sqrt(r*r - i*i);
		N = N + temp;
	}
	cout << N*4;
	system("pause");
	return 0;

}
